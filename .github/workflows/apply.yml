name: Terraform apply

on:
  push:
    branches: 
      - master
  workflow_dispatch:

jobs:
  apply:
    runs-on: self-hosted
    steps:
      - name: Apply
        run: | 
         cd ~/git_work
         git pull origin master
         cd .github/workflows/
         terraform init
         terraform plan
         echo "Done!."
